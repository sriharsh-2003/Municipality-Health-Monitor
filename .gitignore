backend/config.json
backend/__pycache__/
**/__pycache__/
*.pyc
# platform_health.xlsx, app_state.json, and attachments are intentionally
# committed right now so demo data ships with the repo for the client
# preview deploy. Before handing this off for real, wipe the demo data
# and re-add these three lines.
data/backups/*
!data/backups/.gitkeep
